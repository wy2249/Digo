func main(a int, b float) void {
    a = 5

    // mismatch
    // var i,j int = 5
    // Fatal error: exception Failure("assignment mismatch: 2 variables but 1 values")

    // illegale type
    // var x,y int = 5, 6.6
    // Fatal error: exception Failure("illegal assignment type")

    // duplicate name
    var x,y,b int = 5,6,6
    // Fatal error: exception Failure("duplicate local variable declarations")
}

