func main(a int, b float) void {
    
    a = 10
    printInt(a)

    printString("hi")

    var x int

    s1 := "plt"
    printString(s1) // not seemed right

    x = len(s1)
    printInt(x)
    y := x
    // y:= len(s1) // short decl cannot work

    var s2, s3 string = "DIGO", "digo"
    
    s4 := s2 + " test"

    s3 = s2

    bol1 := s3 >= s2
    
    if (bol1) {
        s3 = s1
    }
}


func try_return_one(a int) int {
    
    return a+10
}
/*
func try_return_two(a int,b float) (int,float) {
    
    return a+10,b+20.0
}
*/