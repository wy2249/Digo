func gcd(a int, b int) int {
  for (a != b) {
    if (a > b) {
     a = a - b
    } else {
     b = b - a
    }
  }
  return a
}

func main() void {
  println("%d", gcd(14,21))
  println("%d", gcd(8,36))
  println("%d", gcd(99,121))
}
